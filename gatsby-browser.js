/**
 * use gastby-browser when you want to apply something to the entire site
 */
// setting an alias (wrap) from wrapRootElement
import {wrapRootElement as wrap} from './root-wrapper'

export const wrapRootElement = wrap